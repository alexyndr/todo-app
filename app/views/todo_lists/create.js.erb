$('#blocks .container').last().remove();
$('body #blocks').append("<%= j render @todo_list %>");
$('#blocks .new_list').show().detach().appendTo('#blocks');
$('.footer').remove();
$('#list_<%= @todo_list[:id] %> .items_list').hide();